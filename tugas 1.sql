CREATE TABLE mahasiswa (
id int,
nama VARCHAR(255),
jurusan VARCHAR(255),
kampus VARCHAR(225)
);

CREATE TABLE dosen (
id int,
nama VARCHAR(255)
);

CREATE TABLE matakuliah (
code int,
matkul VARCHAR(255)
);